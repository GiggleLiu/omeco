# Cycle 06 reflection — attempt 031 (Theorem 2)

## Review — what we did

Single-attempt cycle: 031 (improve, parent 026) formalized, proved, and
measured the balance-constrained dyadic-window profile bound. Hypothesis
REFUTED decisively: the bound does not exceed the max-form cap on either
instance (sycamore HC path-DP 47.0 vs cap 53; reg3 30.3 vs 30). The
theorem itself is correct (validated on 3000 random trees; path-DP matches
brute force at n=4..9); the refutation is structural, not computational.

## Lessons we learnt

- **Observation**: every profile-based relaxation — 026's unconstrained
  sum, 031's window-min sum, 031's path-DP — lands at or below the
  bisection width (47 sycamore / 30 reg3), below the carving cap of 53.
  **Root cause**: (1) log-sum-exp collapse: summing J window terms adds at
  most log2(J) ≈ 3 bits over the largest; (2) the profile b(k) dips
  off-center — a triple-verified size-141 sycamore set has boundary
  exactly 40, so temporal slabs are NOT the minimal cuts at intermediate
  sizes; compact spacetime boxes are cheaper. Proven consequence: NO
  function of the isoperimetric profile alone can exceed the bisection
  width; the 47→53 gap lives in the joint nested-cut structure
  (carving-width), invisible to per-size minima. *Confirmed (proof +
  explicit certificates).* **Implication**: the b(k) bound family is
  exhausted — closed with a clean impossibility observation that belongs
  in the paper's theory section ("why the remaining gap resists
  certification"). The certified interval for sycamore remains
  [53, 61.544] via 022's carving argument, which is now provably the
  right bound type, not merely the best found.

### Evidence carried forward

- Theory package complete: Theorem 1 (sum-form, proven, collapses),
  Theorem 2 (dyadic-window, proven, collapses), impossibility observation
  (profile bounds ≤ bisection width), carving bound 53 (022), frontier
  width-optimality (022), profile conservation at the frontier (027).
- b(141) ≤ 40 certificate corrects an assumption from 022's analysis:
  temporal slabs dominate only near balance.
- Research program status: optimizer directions closed (cycles 2–5);
  bound directions closed (026, 031); the write-up is the remaining work.

### Literature check

Profile-vs-carving separation for contraction-cost bounds appears novel;
carving-width connections exist in the graph-minors literature but not
applied to log-sum contraction cost in the project KB. Nothing to add.

## Next round

None planned. Recommendation: proceed to write-up (paper-writer). The
paper: certification arc (width-optimality, [53, 61.544], profile
conservation, impossibility of profile-based improvement) + the sc_target
discovery and library fix + the methodology narrative (three
instrument-caught failure modes across six cycles).
